module.exports = class build {
    constructor() {
        this.name = "build",
        this.alias = [],
        this.usage = "x!build"
    }

    async run(client, message,args) {
        try{
            
        }catch(e) {
            throw e;
        }
    }
}